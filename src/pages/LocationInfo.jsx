import React from "react";
import styled from "styled-components";

const LocationInfo = () => {
  return <Wrapper>오시는 길</Wrapper>;
};

export default LocationInfo;

const Wrapper = styled.div`
  min-height: calc(100vh - 250px);
`;
